# 任务清单

> 当前焦点：T-001（未开始）

- [ ] T-001：补齐交互契约：终端分区 `ViewModel` 与 `UIIntent`
  - 改动点：`packages/contracts/src/index.ts`（新增 `terminalDock`：终端分区视图模型 与相关意图）
  - 技术要点：定义 `layout.kind`、`regions/tabIds/activeTabId`、`splitRatios`；定义 `TERMINAL_DOCK_DROP`（区域内容/标题/标题空白三类落点）与 `TERMINAL_DOCK_SPLIT_RATIO_SET`；补齐注释语义与默认值约束
  - 验证：`pnpm -w typecheck` 通过；右侧终端相关代码可按类型接入
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, AC-001

- [ ] T-002：实现 store：终端分区状态编排与不变量
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/terminalDock.ts`（新增）；`apps/desktop/src/renderer/src/store.ts`（接入 handler）
  - 技术要点：维护 `terminalDock.layout` 与 `regions`；确保不变量：最多 4 区、`regions[i].tabIds.length >= 1`、会话只属于一个区域；实现拖拽落点行为矩阵：分割/移动/合并/交换；会话新增/关闭触发自动回退布局；更新 `activeTabId` 的默认选择规则
  - 验证：新增纯函数单测或最小自测脚本（如 `terminalDockReducer`）覆盖：1→2、2→3、3→4、合并 2→1、交换、关闭回退；`pnpm -w typecheck`
  - 关联需求：REQ-002, REQ-003, REQ-004, REQ-006, REQ-007, AC-002, AC-005, AC-007, AC-008

- [ ] T-003：新增 `TerminalDockView`：渲染 1～4 区布局与标题栏
  - 改动点：`packages/ui-next/src/panels/right/TerminalDockView.tsx`（新增）；`packages/ui-next/src/panels/right/TerminalDockView.module.css`（新增）
  - 技术要点：按 `layout.kind` 渲染 1/2/3/4 区；每区标题栏渲染 `tabIds` 并支持点击切换 `activeTabId`；区域容器内只渲染 1 个 `TerminalView`（传入该区域的 `activeTabId`）；标题可拖拽并显示落点高亮
  - 验证：右侧终端模式下可见多区布局；点击标题切换会话生效；拖拽时高亮落点正确
  - 关联需求：REQ-001, REQ-003, REQ-004, REQ-007, AC-003

- [ ] T-004：改造 `TerminalView` 支持“只渲染一个会话”
  - 改动点：`packages/ui-next/src/panels/right/TerminalView.tsx`
  - 技术要点：将 `TerminalView` 从“遍历 `terminal.panelIds` 全部挂载但只显示 active”改为“只挂载传入的 `sessionId`”；保证现有复制/粘贴/右键粘贴/尺寸自适应逻辑不回归；实例缓存仍按 `sessionId` 复用
  - 验证：单区与多区下输入输出正常；`Ctrl+V` 不重复；切换会话不会丢输出
  - 关联需求：REQ-001, REQ-006, AC-004

- [ ] T-005：RightPanel 接入分区终端：替换 headerTabs 与主体渲染
  - 改动点：`packages/ui-next/src/panels/right/RightPanel.tsx`、`packages/ui-next/src/panels/right/RightPanel.module.css`
  - 技术要点：终端模式下标题栏改为“当前区域的标题栏集合”或“主标题栏 + 分区标题栏”（按设计稿实现）；新增拖拽交互事件派发；保持对话模式不受影响
  - 验证：终端/对话切换正常；新增终端会话仍可用；终端分区渲染正确
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-006, REQ-007, AC-001

- [ ] T-006：实现分隔条拖动与终端尺寸同步
  - 改动点：`packages/ui-next/src/panels/right/TerminalDockView.tsx`、`apps/desktop/src/renderer/src/store/handlers/terminalDock.ts`
  - 技术要点：分隔条用 `pointerdown/move/up` 计算比率写入 `splitRatios`；对每个区域容器加 `ResizeObserver`，变化时派发 `TERMINAL_RESIZE` 给对应 `sessionId`；对比率加最小阈值，避免区域被拖到 0
  - 验证：拖动分隔条时区域实时变化；终端光标/输入正常；松手后尺寸保持
  - 关联需求：REQ-005, AC-004

- [ ] T-007：补齐交互细节与回归清单
  - 改动点：`.specwave/workspace/stories/STORY-000016(右侧终端分区拖拽布局)/02-设计.md`（补充行为矩阵与边界）；（可选）新增 `refs/contracts/terminal-dock.md`
  - 技术要点：明确“拖到区域内容边缘”如何分割；“拖到区域内容中心”如何移动；“合并后标题栏切换”口径；超过 4 会话的处理
  - 验证：按 AC-001～AC-008 逐条走通；补齐最短回归步骤
  - 关联需求：REQ-002, REQ-003, REQ-004, REQ-006, REQ-007, AC-001, AC-008

---
> 模板说明：
> - 每条任务必须包含"做什么 + 改哪里 + 技术要点 + 验证方式"，让新窗口的 AI 能直接执行
> - **关联字段格式**：`关联需求：REQ-xxx, AC-xxx`（逗号分隔，支持中英文冒号和逗号）
> - 关联字段用于 UI 渲染时展示对应的需求和验收口径内容
> - 格式契约详见：`FORMAT-CONTRACT.md`
