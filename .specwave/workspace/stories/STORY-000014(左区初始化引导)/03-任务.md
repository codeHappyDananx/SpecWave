# 任务清单

> 当前焦点：T-007（进行中）

- 注释要求（强制）：
  - 新增 `UIIntent`：界面意图 / `ViewModel`：视图模型 字段时，用块注释说明“触发来源、字段语义、为空语义、失败语义”。
  - 新增 `IPC`：进程间通信 通道时，在 `preload` 与 `main` 两侧都写清：通道名、入参、出参、事件流、失败是否可重试。
  - 初始化状态机在 `store`：状态编排 侧必须有一段“状态迁移说明”（4~8 行），让后来的人不读完整实现也能回归。

- [x] T-001：调研左栏既有「SPECWAVE 工作区」入口与未初始化判定现状
  - 改动点：`packages/ui-next/src/panels/left`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：先定位当前 SPECWAVE 工作区用的 `ViewModel` 字段与渲染组件；确认“未初始化”现在是否已有口径（例如是否已读 `.specwave/settings.json`）；只读 1~2 个入口文件够用就停
  - 验证：能在代码里明确现有入口与状态来源，并给出改造点清单
  - 关联需求：REQ-001, AC-001

- [x] T-002：在 `packages/contracts` 增加初始化引导相关契约字段
  - 改动点：`packages/contracts/src`（现有 `UIIntent`、`AppViewModel` 或相关类型文件）
  - 技术要点：把 `refs/contracts/specwave-init.md` 中的 `UIIntent`：界面意图 与 `InitWizardVM`：引导视图模型 落到契约层；保持字段可扩展但不过度泛化；补齐必要注释（字段语义与失败语义）
  - 验证：`pnpm typecheck` 通过；界面层与 `store`：状态编排 能引用新类型
  - 关联需求：REQ-002, REQ-004, AC-002

- [x] T-003：拆分左栏大文件后再实现初始化引导 UI
  - 改动点：`packages/ui-next/src/panels/left`（既有 SPECWAVE 工作区视图文件）
  - 技术要点：`LeftPanel.tsx` 行数较多，先把 “SpecWave 工作区” 渲染抽到独立组件文件，再把“未初始化卡片 / 初始化弹出框”拆成更小组件；严格只做展示与采集交互；按钮点击通过 `dispatch(intent)`：派发意图；弹出框用 `shadcn/ui`：组件库（`Dialog`：对话框、`Card`：卡片、`Button`：按钮、`Badge`：徽标、`Progress`：进度条、`ScrollArea`：滚动区、`Alert`：提示框 等，优先用 `shadcn mcp`：组件脚手架 获取/引入）；步骤指示区按 `02-设计.md` 的层级与留白口径实现
  - 验证：开发环境打开未初始化项目时能看到引导卡片；点击“初始化”弹出框能打开并展示步骤/进度/日志占位
  - 关联需求：REQ-001, REQ-002, REQ-004, AC-001, AC-002

- [x] T-004：store 接入初始化意图并驱动 `InitWizardVM` 状态机
  - 改动点：`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：`store.ts` 体量较大，新增状态机优先下沉到独立 `handler`：处理器 文件；处理 `SPECWAVE_INIT_OPEN/START/RETRY/CLOSE/COPY_ERROR`；把运行中/成功/失败映射成 `InitWizardVM.phase`；追加日志行时做上限与节流；成功后触发左栏工作区刷新；补齐“状态迁移说明”注释
  - 验证：点击“初始化/重试/关闭”能正确改变界面状态；失败态能展示错误与“可重试”
  - 关联需求：REQ-002, REQ-003, AC-002, AC-003, AC-004

- [x] T-005：运行时复用 `CLI` 初始化能力并提供结构化进度事件
  - 改动点：`apps/desktop/src/preload`、`apps/desktop/src/main`（或项目中现有初始化/命令执行模块）
  - 技术要点：优先复用同一初始化入口（函数/模块）；通过 `IPC`：进程间通信 产出结构化事件：步骤状态、百分比、日志追加；失败返回结构化错误（标题/详情/可重试/可复制文本）；为新通道补齐注释（入参/出参/失败语义）
  - 验证：初始化过程会持续推送进度与日志；成功/失败都能被界面稳定消费；与 `CLI` 产物一致
  - 关联需求：REQ-003, AC-003, AC-004, AC-005

- [x] T-006：补齐 `shadcn/ui` 组件依赖与样式口径收敛
  - 改动点：`packages/ui-next/src/primitives/shadcn`（缺什么补什么）、相关 UI 文件
  - 技术要点：通过 `shadcn`：脚手架 方式引入组件，集中落在 `primitives/shadcn`；避免通用类名外溢；弹出框的按钮层级与间距按 `ui-ux-pro-max`：界面体验技能 建议收敛
  - 验证：组件不散落；样式不污染其他区域；视觉层级清晰
  - 关联需求：REQ-004, AC-002

- [ ] T-007：更新项目结构图与提交代码（不推送）
  - 改动点：`.specwave/workspace/project-map.md`、git
  - 技术要点：如果新增/调整了职责或入口，补一条“做什么/依赖谁/被谁依赖/边界备注”；按仓库门禁只提交不推送
  - 验证：`git status` 干净；有本次改动提交记录
  - 关联需求：AC-005

- [x] T-008：优化 `specwave-skills` 的「新建需求」读取门禁口径
  - 改动点：`specwave-skills/resources/packs/core/light/.specwave/prompts/新建需求.md`
  - 技术要点：需求阶段允许只读调研项目源码/配置，保留“禁止读非当前 Story”的约束；补充“禁写入/禁装依赖/禁生成产物”的硬规则
  - 验证：pack 文件内容更新；新初始化项目后提示口径不再误导为“只能读最小集合”

---
> 说明：执行阶段开始后，严格按本清单逐条做，不自行扩展。
