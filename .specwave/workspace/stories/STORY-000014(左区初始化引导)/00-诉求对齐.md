# 诉求对齐（STORY-000014）

## 目标
不新增左栏模块：在左栏既有「SPECWAVE 工作区」内补齐“未初始化态”与“初始化引导”能力：
- 已初始化：保持现有内容，同时补充清晰的状态提示
- 未初始化：用引导卡片替换/覆盖现有内容，并提供「初始化」按钮

## 触发与流程（对齐到 `CLI`：命令行）
- 点击「初始化」后，弹出引导框：逐步展示初始化进度与结果
- 初始化执行逻辑复用与 `CLI` 相同的底层能力（后续在设计阶段确认复用点与命令）

## 交互与视觉（用 `shadcn/ui`：组件库）
- 左栏：卡片式模块，顶部固定标题「SPECWAVE」+ 状态徽标
- 弹出框：分步指示区（步骤 + 进度 + 日志摘要），失败给出可复制的错误信息

## 待确认口径（默认值可改）
- 未初始化判定：当前项目根目录缺少 `.specwave/settings.json`
- 初始化完成判定：生成 `.specwave/settings.json` 且可读取 `currentSession`
