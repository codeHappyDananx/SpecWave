# 任务清单

> 当前焦点：无（已完成，可归档）

- [x] T-001：补齐交互契约（左区 tab + 能力视图 + 安装/刷新）
  - 改动点：`packages/contracts/src/index.ts`
  - 技术要点：新增左区 tab 字段、`CodexCapabilitiesVM`：能力视图模型、`MCP`/`skills` 状态结构；新增 `UIIntent`：界面意图（切换 tab、刷新、安装 MCP、安装技能）；保持“先改 contracts，再改实现”
  - 验证：`pnpm typecheck` 通过；`apps/desktop` 与 `ui-next` 无类型报错
  - 关联需求：REQ-001, REQ-003, REQ-004, REQ-005, REQ-007, REQ-008, AC-001, AC-003

- [x] T-002：renderer `store` 编排能力视图状态与意图处理
  - 改动点：`apps/desktop/src/renderer/src/store.ts`（接线），新增独立模块（例如 `apps/desktop/src/renderer/src/store/handlers/codexCapabilities.ts`）
  - 技术要点：处理 `LEFT_PANEL_TAB_SET`、`CODEX_CAPABILITIES_REFRESH`、安装相关意图；生成 `AppViewModel` 的能力分组数据；切换回工作区时保留原左区浏览状态（不重置 explorer 内部状态）
  - 验证：启动应用后切换“工作区/能力”不会导致左区树重置；刷新进入“检测中”再更新
  - 关联需求：REQ-002, REQ-003, REQ-004, REQ-005, AC-002, AC-004, AC-005

- [x] T-003：主进程实现 `codex` 能力探测（官方命令）与脱敏
  - 改动点：新增独立模块（例如 `apps/desktop/src/main/codexCapabilities/*`），`apps/desktop/src/main/ipc.ts`（注册 IPC）
  - 技术要点：
    - 配置快照：通过官方 `CLI`：命令行界面 `codex mcp list --json` 获取 `MCP` 配置列表
    - 脱敏：不返回任何 `env`：环境变量 明文值；只返回键名（参考 `refs/contracts/codex-capabilities.v1.md`）
    - 超时与回收：外部命令必须有超时与进程回收策略
  - 验证：能力视图展示 `MCP` 列表；任意 `env` 值不会出现在 UI/日志里
  - 关联需求：REQ-004, REQ-006, AC-004, AC-007

- [x] T-004：主进程实现 `MCP` 连通性探测（官方 `SDK`）
  - 改动点：同 T-003 模块内新增探测子模块；必要时新增依赖（以官方文档为准）
  - 技术要点：
    - 使用官方 `MCP`：模型上下文协议 `SDK`：软件开发包 按生命周期握手（`initialize` → `initialized`），可选再调用 `tools/list`
    - `stdio`：标准输入输出 传输需要拉起子进程；必须确保失败可回收
    - 状态映射为 `ok/error/unknown/checking`，并提供可理解且脱敏的原因
  - 验证：至少 1 个已配置 `MCP` 能显示“握手成功/失败原因”；探测失败不会卡死 UI
  - 关联需求：REQ-004, AC-004, AC-006

- [x] T-005：preload 暴露能力探测与安装桥接（只做桥接）
  - 改动点：`apps/desktop/src/preload/index.ts`（新增 `window.specwave.codexCapabilitiesProbe`、安装方法），必要时新增类型定义文件
  - 技术要点：preload 只负责参数校验与 IPC 调用；不在 preload 内做文件系统写入与命令执行
  - 验证：renderer 可调用探测与安装方法拿到结果；preload 无未捕获异常
  - 关联需求：REQ-004, REQ-007, REQ-008, AC-005

- [x] T-006：左区 UI 新增常驻切换条（遵从 Flat + `shadcn/ui`）
  - 改动点：新增独立组件（例如 `packages/ui-next/src/panels/left/LeftRailSwitcher.tsx`），`packages/ui-next/src/panels/left/LeftPanel.tsx` 最小接线
  - 技术要点：优先复用现有 primitives：`Tab`：页签 承载“工作区/能力”二态；`IconButton`：图标按钮 承载刷新/安装；悬停详情用 `HoverCard`：悬浮卡片（右侧展开）；激活态用色块与字色变化；通过 `className` 覆盖去阴影（例如 `shadow-none`）；样式口径遵从 `SPECWAVESTYLE.MD`
  - 验证：左区最左侧出现常驻切换条；悬停有详情卡片；点击可切换视图
  - 关联需求：REQ-001, AC-001

- [x] T-007：能力视图 UI（列表渲染 + 状态展示）
  - 改动点：新增目录（例如 `packages/ui-next/src/panels/left/codexCapabilities/*`），渲染 `CodexCapabilitiesVM`
  - 技术要点：优先复用现有 primitives：`SearchInput`：搜索输入 做筛选；`SidebarGroup/SidebarMenu`：侧栏分组列表 做分组渲染；状态用 `Badge`：徽标；内容滚动用 `ScrollArea`：滚动区域；全局失败用 `Alert`：提示条；加载态可用 `SidebarMenuSkeleton`：列表骨架；所有容器统一通过 `className` 去阴影（`shadow-none`）
  - 验证：进入“能力”不空白；`MCP` 与 `skills` 分组可滚动展示；失败不影响其他项
  - 关联需求：REQ-003, REQ-004, REQ-005, AC-003, AC-006

- [x] T-008：`MCP` 安装 UI（JSON 输入 + 预览 + 确认）
  - 改动点：能力视图内新增安装弹窗组件（`Dialog`：对话框）；复用 `packages/ui-next/src/primitives/shadcn/textarea.tsx` 的 `Textarea`：多行输入框
  - 技术要点：弹窗与输入框通过 `className` 覆盖去阴影（例如 `DialogContent` 默认 `shadow-lg`、`Textarea` 默认 `shadow-xs`）；粘贴 `JSON` 后做本地校验与脱敏预览；确认后派发 `CODEX_MCP_INSTALL_FROM_JSON`；安装中禁用按钮；成功后自动刷新
  - 验证：粘贴合法 `JSON` 可安装；非法 `JSON` 给出明确提示；安装成功后列表出现新条目
  - 关联需求：REQ-007, AC-008

- [x] T-009：`skills` 安装 UI（zip / md / 目录）
  - 改动点：能力视图内新增安装弹窗；接入系统文件选择（通过 preload）
  - 技术要点：支持选择 `zip` 或 `md`，并可选“选择目录”；预览显示解析到的技能名称与目标范围（用户级/项目级）；确认后派发 `CODEX_SKILL_INSTALL`
  - 验证：安装成功后技能列表出现新条目；缺少 `SKILL.md` 会提示原因
  - 关联需求：REQ-008, AC-009

- [x] T-010：主进程实现 `MCP` 安装（JSON → 官方 `codex mcp add`）
  - 改动点：`apps/desktop/src/main/codexCapabilities/*`
  - 技术要点：
    - 解析输入 `JSON`，按契约规则映射到官方命令 `codex mcp add`
    - 支持 `stdio` 与 `http` 两类传输；`env` 通过 `--env KEY=VALUE` 传入
    - 同名覆盖：提示并实现为 `codex mcp remove <name>` + `codex mcp add ...`
    - 全链路脱敏：错误信息不回显明文口令
  - 验证：安装后 `codex mcp list --json` 能看到新增条目；UI 刷新后展示该条目
  - 关联需求：REQ-007, AC-008, AC-007

- [x] T-011：主进程实现 `skills` 安装（zip / md / 目录 → 官方发现目录）
  - 改动点：`apps/desktop/src/main/codexCapabilities/*`
  - 技术要点：
    - 目标目录：用户级 `~/.codex/skills`（默认），可选项目级 `$CWD/.codex/skills`
    - `zip`：解压并归一化为技能目录（必须含 `SKILL.md`）；失败时清理半成品
    - `md`：单文件则创建技能目录并写入 `SKILL.md`；目录内 `SKILL.md` 则复制该目录全部内容
    - 覆盖策略：同名目录存在时提示并可覆盖（先删再写）
  - 验证：安装后在目标目录可见；能力视图刷新后出现新技能条目
  - 关联需求：REQ-008, AC-009

- [x] T-012：补齐 `project-map` 与回归说明
  - 改动点：`.specwave/workspace/project-map.md`，以及必要的说明文档
  - 技术要点：新增/改动文件职责必须同步更新项目结构真相源；明确“能力视图”入口、数据来源与边界
  - 验证：`project-map.md` 中能找到新增模块条目，职责/依赖/边界清晰
  - 关联需求：REQ-006, AC-003

- [x] T-013：验证与提交（不 `push`）
  - 改动点：运行与回归命令；`git`：版本管理 提交
  - 技术要点：
    - 本地验证：`pnpm typecheck`；`pnpm dev` 运行后按 `AC-001~AC-009` 冒烟
    - 安全检查：确认 UI/日志无敏感值；安装失败不会遗留半成品目录
    - 提交：把所有已跟踪变更（含删除）一次性提交，提交信息包含 `STORY-000017`
  - 验证：工作区干净；功能可用；未 `push`
  - 关联需求：AC-001, AC-002, AC-003, AC-004, AC-005, AC-006, AC-007, AC-008, AC-009

- [x] T-014：左区切换条悬浮详情（右侧展开）
  - 改动点：`packages/ui-next/src/panels/left/LeftRailSwitcher.tsx`，新增 `HoverCard`：悬浮卡片组件 `packages/ui-next/src/primitives/shadcn/hover-card.tsx`
  - 技术要点：悬浮详情统一用 `HoverCard`：悬浮卡片，`side="right"`；样式遵从 Flat（禁阴影），仅用色块 + `border-2` 做结构
  - 验证：鼠标移到“工作区/能力”图标时，右侧出现详情卡片；点击仍可正常切换
  - 关联需求：REQ-001, AC-001

---
> 模板说明：
> - 每条任务必须包含"做什么 + 改哪里 + 技术要点 + 验证方式"，让新窗口的 AI 能直接执行
> - **关联字段格式**：`关联需求：REQ-xxx, AC-xxx`（逗号分隔，支持中英文冒号和逗号）
> - 关联字段用于 UI 渲染时展示对应的需求和验收口径内容
> - 格式契约详见：`FORMAT-CONTRACT.md`
