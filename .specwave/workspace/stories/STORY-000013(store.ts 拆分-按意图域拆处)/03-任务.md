# 任务清单

> 当前焦点：T-009（进行中）

- [x] T-001：建立拆分骨架（store 目录 + handler 路由 + 共享工具）
  - 改动点：`apps/desktop/src/renderer/src/store.ts`、`apps/desktop/src/renderer/src/store/*`
  - 技术要点：
    - 新建 `apps/desktop/src/renderer/src/store/handlers/`，按意图域放 handler 文件
    - 新建 `apps/desktop/src/renderer/src/store/shared/`，收纳跨域复用的工具（如 path/layout）
    - 定义统一 handler 入口签名（`StoreCtx(set/get/dispatch)` + `HandlerResult`），避免每个 handler 自创口径
    - 在 `store.ts` 保持唯一 `dispatch(intent)` 出口不变，先把路由层搭起来（此任务不搬任何业务 case）
  - 验证：
    - `pnpm typecheck`
    - 应用可启动（`pnpm dev`），基础 UI 可渲染（不要求交互全量验证）
    - 已执行：`pnpm typecheck` 通过（顺带修了一个阻塞 typecheck 的 TS2538）
  - 关联需求：REQ-001, REQ-002, REQ-005, AC-002

- [x] T-002：拆出 Panel handler（面板开关/模式切换/右侧面板新增）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/panel.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`PANEL_TOGGLE_LEFT`、`PANEL_TOGGLE_CENTER`、`PANEL_TOGGLE_RIGHT`、`RIGHT_MODE_SET`、`LEFT_VIEW_MODE_SET`、`RIGHT_PANEL_ADD`
    - 保留 `normalizeLayoutStable` 的调用时机与返回结构，避免布局时序变化
    - 在文件头写清：本 handler 读写哪些 VM 字段、是否有副作用、有哪些边界条件（注释口径统一）
  - 验证：
    - `pnpm typecheck`
    - 冒烟：打开应用后切换左/中/右栏可见性；切换右侧模式；新增右侧面板（不报错、布局不乱）
    - 已验证：左/中/右栏开关、右侧模式切换、右侧新增面板
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [x] T-003：拆出 Layout handler（拖拽与容器宽度）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/layout.ts`、`apps/desktop/src/renderer/src/store/shared/layout.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`LAYOUT_CONTAINER_SET`、`LAYOUT_DRAG_START`、`LAYOUT_DRAG_MOVE`、`LAYOUT_DRAG_END`
    - `DragSnapshot` 与布局计算逻辑收敛到 `shared/layout.ts`，供 panel/layout 复用，避免重复实现
    - 注释写清：拖拽快照字段语义、deltaX 处理与边界（最小宽度）策略
  - 验证：
    - `pnpm typecheck`
    - 冒烟：拖拽左右分隔条，宽度变化稳定，松手后布局被归一化且不抖动
    - 已执行：`pnpm typecheck` 通过
    - 已验证：拖拽左右分隔条；松手后布局归一化；缩放窗口不抖动
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [x] T-004：拆出 Theme handler（主题与皮肤持久化）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/theme.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`THEME_TOGGLE`、`SKIN_CYCLE`
    - 保留 localStorage key 与读写时机（`specwave_theme` / `specwave_skin`），避免偏好丢失
    - 注释写清：切换策略（循环/枚举）与失败兜底（try/catch）
  - 验证：
    - `pnpm typecheck`
    - 冒烟：切换 theme/skin 生效；刷新窗口偏好仍能被正确加载
    - 已执行：`pnpm typecheck` 通过
    - 已验证：浅色/深色切换、主色切换；刷新后偏好仍能恢复
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [x] T-005：拆出 Project handler（项目 tabs + 最近项目 + 全局搜索）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/project.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`PROJECT_TAB_ADD_EMPTY`、`PROJECT_SELECT`、`PROJECT_OPEN_RECENT`、`RECENT_PROJECT_REMOVE`、`PROJECT_TAB_SET_ACTIVE`、`PROJECT_TAB_CLOSE`、`GLOBAL_SEARCH_SET`
    - 维持 sessionStorage 的 projects 会话恢复（`PROJECTS_SESSION_KEY`）语义与时机，避免“刷新吞掉新开的项目”
    - 相关 helper（tab 选择、最近激活记录）随 handler 迁移或下沉 `shared/`
  - 验证：
    - `pnpm typecheck`
    - 冒烟：新增/关闭 tab；切换 active tab；打开 recent；刷新后 openTabs/activeTabId 仍能恢复
    - 已执行：`pnpm typecheck` 通过
    - 已验证：新增空项目页签时，中区头部不再残留上一个 Story 的步骤条（`storyStepper`）
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [x] T-006：拆出 Explorer handler（文件树/打开文件/在系统中定位）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/explorer.ts`、`apps/desktop/src/renderer/src/store/shared/path.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`EXPLORER_SHOW_IGNORED_SET`、`EXPLORER_TOGGLE_DIR`、`EXPLORER_OPEN_FILE`、`EXPLORER_REVEAL_IN_OS`
    - path 工具（join/basename/dirname/normalizeFsPath）下沉 `shared/path.ts`，避免各 handler 复制
    - 维持 explorer 节点更新算法（merge/updateNodeById/findNodeById）行为一致
  - 验证：
    - `pnpm typecheck`
    - 冒烟：展开/收起目录；打开文件后内容区同步变化；“在资源管理器中显示”可用（或至少不报错）
    - 已验证：展开/收起目录可用；打开文件后中区内容同步；“在资源管理器中显示”不报错
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [ ] T-007：拆出 Content handler（编辑/保存/查找）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/content.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`CONTENT_TOGGLE_VIEW_MODE`、`CONTENT_DRAFT_SET`、`CONTENT_SAVE_REQUEST`、`CONTENT_FIND_SET_QUERY`、`CONTENT_FIND_NEXT`、`CONTENT_FIND_PREV`、`CONTENT_FIND_CLOSE`
    - 维持 saveStatus 状态机与错误处理（saving/saved/error/conflict）语义不变
    - 查找相关 helper（`findMatchStarts`、activeIndex 轮转）随 handler 迁移
  - 验证：
    - `pnpm typecheck`
    - 冒烟：编辑内容后 isDirty/saved 状态正确；保存成功与失败提示一致；查找 next/prev 可用
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [x] T-008：拆出 Task handler（任务板/详情/关联文档跳转）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/task.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`TASK_ITEM_TOGGLE`、`TASK_ITEM_OPEN`、`TASK_DETAIL_OPEN`、`TASK_DETAIL_CLOSE`、`TASK_DETAIL_MODE_SET`、`TASK_DETAIL_DRAFT_SET`、`TASK_DETAIL_SAVE`、`TASK_ITEM_START`、`TASK_DECK_MODE_SET`、`TASK_DECK_PREV`、`TASK_DECK_NEXT`、`TASK_DECK_FOCUS`、`TASK_LINKED_DOC_JUMP`、`TASK_LINKED_DOCS_TOGGLE_SECTION`
    - 任务解析/草稿提取/linked docs 相关 helper 随 handler 迁移（必要时下沉 `shared/`）
    - 注释写清：任务状态更新点、异步加载 linked docs 的失败语义与重试策略
  - 验证：
    - `pnpm typecheck`
    - 冒烟：勾选任务、打开详情、切换 deck；linked docs 加载/跳转可用（或失败提示一致）
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [ ] T-009：拆出 Terminal handler（面板/输入输出/事件回写）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/terminal.ts`、`apps/desktop/src/renderer/src/store.ts`、`packages/ui-next/src/panels/right/TerminalView.tsx`
  - 技术要点：
    - 覆盖 intent：`TERMINAL_PANEL_CLOSE`、`TERMINAL_PANEL_SET_ACTIVE`、`TERMINAL_WRITE`、`TERMINAL_RESIZE`、`TERMINAL_COPY`、`TERMINAL_PASTE`
    - 保留终端输出回写的时序与截断策略（outputByPanel 的增长控制），避免输出丢行
    - 注释写清：terminal panelId 生命周期、用户输入缓存（typed set）语义
  - 验证：
    - `pnpm typecheck`
    - 冒烟：打开终端可输入；copy/paste 可用；终端输出能持续追加且不乱序
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [ ] T-010：拆出 Chat handler（会话/草稿/提交）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/chat.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`CHAT_SESSION_CLOSE`、`CHAT_SESSION_SET_ACTIVE`、`CHAT_DRAFT_SET`、`CHAT_MESSAGE_SUBMIT`
    - 维持 messagesBySession/draftBySession 的读写语义与默认值策略
    - 注释写清：提交后的消息追加顺序与清空草稿策略
  - 验证：
    - `pnpm typecheck`
    - 冒烟：切换会话、编辑草稿、提交消息；会话关闭后 activeSessionId 兜底正确
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [ ] T-011：拆出 Story handler（看板/阶段跳转）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/story.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`STORY_BOARD_LOAD`、`STORY_BOARD_REFRESH`、`STORY_CARD_CLICK`、`STORY_CARD_SELECT`、`PHASE_INDICATOR_CLICK`、`STORY_STEPPER_PHASE_CLICK`
    - 保留 `queueMicrotask(() => dispatch(EXPLORER_OPEN_FILE))` 的跳转时序，避免点击阶段后路径错乱
    - Story 扫描/解析（progress、title、phase、context）相关 helper 随 handler 迁移
  - 验证：
    - `pnpm typecheck`
    - 冒烟：Story 列表可加载；点击卡片/阶段能打开对应文件；进度数显示与拆分前一致
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [ ] T-012：拆出 Shortcut handler（保存/查找）
  - 改动点：`apps/desktop/src/renderer/src/store/handlers/shortcut.ts`、`apps/desktop/src/renderer/src/store.ts`
  - 技术要点：
    - 覆盖 intent：`SHORTCUT_SAVE`、`SHORTCUT_FIND`
    - 若快捷键逻辑与 project/content 绑定较深，优先“搬走原逻辑”不重写
    - 注释写清：保存范围（本地/项目）与查找结果更新点
  - 验证：
    - `pnpm typecheck`
    - 冒烟：触发保存/查找入口不报错；快捷键相关 UI 状态能更新
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, AC-003

- [ ] T-013：收尾清理与稳定性回归（缩减 store.ts + 更新项目路径图 + 提交）
  - 改动点：`apps/desktop/src/renderer/src/store.ts`、`.specwave/workspace/project-map.md`、git
  - 技术要点：
    - 删除已迁移的冗余代码与未使用 import，让 `store.ts` 回到“入口 + 路由 + 少量 glue”的职责
    - 更新 `.specwave/workspace/project-map.md`：补充 `apps/desktop/src/renderer/src/store/*` 的职责与边界（新增文件必须补条目）
    - 提交 git（不 push），提交信息带上 `STORY-000013`
  - 验证：
    - `pnpm typecheck`
    - `pnpm dev` 启动并走一遍常用路径：切换三栏/打开项目与文件/编辑与查找/任务板/终端/对话/主题切换
    - `git status -sb` 干净
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, AC-001, AC-002, AC-004

---
> 模板说明：每条任务必须包含"做什么 + 改哪里 + 技术要点 + 验证方式"，让新窗口的 AI 能直接执行。
