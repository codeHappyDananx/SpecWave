# 任务清单

> 当前焦点：T-004（未开始）

- [x] T-001：收紧「新建需求」提示词：先读模板 + 读取白名单 + 禁读历史 + 违规惩罚
  - 改动点：`specwave-skills/resources/packs/core/light/.specwave/prompts/新建需求.md`
  - 技术要点：
    - 强制顺序：先读 `.specwave/templates/01-需求.md`（以及后续阶段对应模板），再开始落盘
    - 明确允许读取白名单（settings/AGENTS/roles/project-map/templates/当前 Story），其余默认不读
    - 明确禁读历史：禁止读取 `.specwave/workspace/stories/` 下非当前 Story 的任何文件
    - 明确惩罚动作：一旦违规读历史 → 立刻停止 + 标记违规 + 回到模板重写当前阶段文档
    - 明确阶段单步推进：本轮只做“诉求对齐”，完成后只让用户回复“继续”推进
  - 验证：
    - `node specwave-skills/bin/specwave.js catalog --format text` 能正常输出（资源仍可被 CLI 识别）
    - `rg \"先读.*templates/01-需求\" specwave-skills/resources/packs/core/light/.specwave/prompts/新建需求.md` 命中
  - 验证结果：通过（catalog 正常输出；rg 命中）
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-005, AC-001

- [x] T-002：强化「需求分析师」角色：每阶段先读对应模板 + 禁读历史 + 禁止揉阶段
  - 改动点：`specwave-skills/resources/packs/core/light/.specwave/roles/需求分析师.md`
  - 技术要点：
    - 在阶段 2/3/4 的“行为规范”里显式写出：落盘前必须读取对应模板（01/02/03）
    - 增加硬规则：禁止在“设计方案”阶段提前输出 `03-任务.md`；必须等用户确认后再拆任务
    - 增加硬规则：默认禁止读取历史 Story，只有用户点名 `STORY-xxxxxx` 才允许读取并说明用途
    - 把“违规惩罚动作”写成可执行步骤（停止/标记/回滚到模板落盘）
  - 验证：
    - `node specwave-skills/bin/specwave.js catalog --format text` 能正常输出
    - `rg \"禁止在.*设计.*输出.*03-任务\" specwave-skills/resources/packs/core/light/.specwave/roles/需求分析师.md` 命中
  - 验证结果：通过（catalog 正常输出；rg 命中）
  - 关联需求：REQ-001, REQ-002, REQ-003, REQ-005, AC-002, AC-003, AC-005

- [x] T-003：把关键门禁下沉到项目根模板：AGENTS 强制“按模板/禁读历史/逐步确认/违规惩罚”
  - 改动点：`specwave-skills/resources/packs/core/light/project-root/AGENTS.md.template`
  - 技术要点：
    - 在 spec 模式硬规则区写死：落盘必须按 `.specwave/templates/*`；默认禁读历史 Story
    - 写死阶段推进：每次只完成一个阶段产物；写完 `02-设计.md` 必须等用户“继续”确认后才能写 `03-任务.md`
    - 写明违规惩罚（与 role/prompt 一致），避免不同文件口径打架
  - 验证：
    - `node specwave-skills/bin/specwave.js catalog --format text` 能正常输出
    - 初始化到新项目后，根 `AGENTS.md` 可见上述硬规则（人工抽查）
  - 验证结果：通过（catalog 正常输出）
  - 关联需求：REQ-002, REQ-003, REQ-004, REQ-005, AC-002, AC-003, AC-005

- [ ] T-004：完成后提交（不 push）
  - 改动点：git
  - 技术要点：只提交 `specwave-skills/resources/packs/core/light` 下相关文件与本 Story 文档，不夹带无关改动
  - 验证：`git status -sb` 干净（工作区无未提交变更）
  - 关联需求：AC-001, AC-002, AC-003, AC-004, AC-005

---
> 模板说明：每条任务必须包含"做什么 + 改哪里 + 技术要点 + 验证方式"，让新窗口的 AI 能直接执行。
