## 任务清单（给开发用）

> 当前焦点：T-006（未开始）
>
> 这份文件偏“开发执行清单”。如果你只想评审效果，优先看 `01-需求.md`。本 Story 只改左栏，不包含顶部工具条与右栏终端区。
>
> 约定：每条任务都是可勾选项；做完就勾上，并补齐“怎么验证”的证据（需要复用的材料放 `refs/`）。

## 清单

- [x] T-001 补齐“显示被忽略项”开关的状态与动作（对应：REQ-004/REQ-006，AC-004/AC-006）
  - 状态：已完成（2026-01-05）
  - 在 `packages/contracts` 增补一个“是否显示被忽略项”的状态字段（用于让 UI 渲染开关当前状态）。
  - 增补一个 `UIIntent`：用于让 UI 把“我点了开关”这件事告诉 `store`（由 `store` 负责保存状态、并影响文件树过滤）。
  - “读目录失败”怎么展示先定一个统一口径：先沿用节点上已有的 `ExplorerNodeVM.error`（就是错误文本）直接展示；如果你希望把“无权限”和“其他错误”分开展示，再补字段。
  - 验证：按 `01-需求.md` 的 AC-004/AC-006 走一遍，截图或录屏放 `refs/`。

- [x] T-002 在 `store` 里实现过滤与持久化（对应：REQ-003/REQ-004，AC-002/AC-003/AC-004）
  - 状态：已完成（2026-01-05）
  - 在 `apps/desktop/src/renderer/src/store.ts` 实现：
    - “显示被忽略项”开关的读写与持久化；
    - 目录读取结果的过滤：默认把噪音项过滤掉，开关打开后不过滤；
    - 打开文件/选中态与左栏联动保持一致（保持既有行为）。
  - 验证：按 `01-需求.md` 的 AC-002/AC-003/AC-004 走一遍，截图或录屏放 `refs/`。

- [x] T-003 通过 shadcn MCP 引入 Sidebar 源码并做样式适配（对应：REQ-001/REQ-005/REQ-006，AC-001/AC-005/AC-006）
  - 状态：已完成（2026-01-05）
  - 通过 shadcn MCP 拉取 Sidebar 相关源码进入 `packages/ui-next/src/primitives`（或其子目录）。
  - 在 `packages/ui-next` 引入 Tailwind，用来直接承载 `shadcn/ui` Sidebar 的现成样式写法。
  - 保持样式边界：不额外引入会覆盖全局的基础样式；现有 reset/tokens 仍然是主口径。
  - 验证：按 `01-需求.md` 的 AC-001/AC-005/AC-006 走一遍，截图放 `refs/`；并在 `05-规则.md` 记录上游来源与本仓库改动点。

- [x] T-004 替换左栏渲染为 Sidebar 形态（对应：REQ-002/REQ-003/REQ-005，AC-001/AC-002/AC-005）
  - 状态：已完成（2026-01-05）
  - 重写左栏文件树与分组区域：用 Sidebar 组件组织结构，树节点统一用 Sidebar item 形态渲染。
  - 补齐键盘可用性（Tab/Enter）。
  - 验证：按 `01-需求.md` 的 AC-001/AC-002/AC-005 走一遍，截图或录屏放 `refs/`。

- [x] T-005 同步结构真相源（规则：改动即更新）
  - 状态：已完成（2026-01-05）
  - 若新增/调整了 primitives 或 left panel 的职责/入口，同步更新 `.specwave/workspace/project-map.md` 对应条目。
  - 验证：打开 `project-map.md` 检查“职责/边界/依赖”是否与实际一致。

- [ ] T-006 走验收（按 `01-需求.md` 的验收条目逐条对照）
  - 状态：未开始
  - 按“AC-001 ~ AC-006”给出：通过/不通过 + 证据（步骤 + 截图/录屏或日志）。
