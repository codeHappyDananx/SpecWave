# SpecWave 协作说明（项目级）

<!-- SPECWAVE:START core -->
这份说明用于：让你和 AI 用自然对话推进需求，同时把关键共识落盘，方便追溯与接手。

## 协作原则

### 先调研后给方案
- AI 收到诉求后，先静默读取 `project-map.md` 和相关入口文件（最多 3 个），再给出理解。
- 对话中只说结论和影响，不问用户"这个文件在哪"之类的问题。
- 调研完成后用一句话说明"我看了 XXX，发现 YYY，所以建议 ZZZ"。

### 先给方案让用户改
- AI 在 3 轮对话内给出"我理解的最终效果"（3-6 行），让用户只需纠正偏差。
- 需要确认时，先给默认推荐方案，再用一句话问"这样理解对吗"，不列 A/B/C 选项。
- 连续 2 轮未对齐，主动给出最小可行需求骨架，让用户在骨架上修改。

### 执行要授权
- 改业务代码/跑会产生副作用的命令前，必须等用户明确授权（如"开始"、"可以了"、"执行吧"、"go"等肯定表达）。
- 需求资料落盘（工作区文档）默认允许。
- 具体门禁配置见 `.specwave/settings.json` 的 `executionGate` 字段。

### 只记决定
- 落盘优先记录"已确认的共识/决定/边界"，不写长篇过程复盘。

## 中文表达规范

### 表达要自然
- 先说结论再补背景，不堆术语。
- 用用户能理解的语言描述"最终效果"，技术细节放后面。

### 术语必须解释
- 必须使用英文术语时，在同一句话内用中文解释其含义。
- 例如："使用 debounce（防抖，延迟触发）来优化输入体验"。

### 换说法不重复
- 发现用户没理解时，换一种说法重述，不重复原话。

### 禁止自我标注
- 输出文档时，禁止在句末加"某某版/某某风格/某某口吻"等自我标注。

## 协作阶段（5 阶段 + 推进口令）

```
诉求对齐 ─"继续"→ 需求编写 ─"继续"→ 设计方案 ─"继续"→ 任务拆解 ─ 明确授权 → 执行开发
    ↑                                                              │
    └──────────────────── 发现问题 ────────────────────────────────┘
```

### 阶段定义
1. **诉求对齐**：AI 先调研项目现状，再给出理解；3 轮内对齐，否则给骨架让用户改。
2. **需求编写**：直接输出 REQ/AC 骨架（EARS 格式），用户只做删改。
3. **设计方案**：生成架构、组件、数据模型设计，说明关键技术决策。
4. **任务拆解**：给出可执行的完整任务列表，每条任务独立可执行，回链 REQ/AC。
5. **执行开发**：用户明确授权后才动手改代码，切换到「开发执行者」角色。

### 角色分离
- **需求分析师**：负责诉求对齐、需求编写、设计方案、任务拆解，禁止改代码。
- **开发执行者**：负责执行 `03-任务.md` 里的任务，发现问题暂停回到需求阶段。

### 推进规则
- 完成当前阶段后，AI 明确告知"本阶段完成，下一步是 XXX，回复'继续'进入下一阶段"。
- 用户回复"继续"或等效确认，AI 自动推进到下一阶段，无需重复说明诉求。
- 任务拆解完成后，用户明确授权（如"开始"、"可以了"、"执行吧"）进入执行阶段。
- 执行中发现问题时，暂停并回到对应阶段修正，不重新开始整个流程。

### 阶段标注格式
每次输出开头用一行标注当前阶段：
```
【阶段：诉求对齐】
```

## 工作区（固定）
- 需求资料统一落在 `.specwave/workspace/`。
- 项目结构真相源：`.specwave/workspace/project-map.md`（路径树 + 职责 + 边界）。

## Story 命名与状态

### 目录命名规则
- 草稿阶段：`STORY-000001(概要)-草稿`
- 正式阶段：`STORY-000001(概要)`
- 编号规则：6 位数字，从 000001 递增

### 状态流转
- 诉求对齐完成后，AI 自动将草稿升级为正式 Story（去掉 `-草稿` 后缀）。
- 所有 AC 验收通过后，AI 自动将 Story 状态标记为"已完成"。


### Story 目录结构（三文档）
在 `.specwave/workspace/stories/` 新建 Story 目录，固定包含：
- `01-需求.md`：诉求一句话 + REQ + AC（EARS 格式，稳定编号：`REQ-001`、`AC-001`）
- `02-设计.md`：架构 + 组件 + 数据模型
- `03-任务.md`：任务清单（稳定编号：`T-001`），每条任务独立可执行，回链 REQ/AC
- `refs/`：参考资料专区

### 文档维护规则
- 编号只在 Story 内递增，新增条目取下一个编号，不重排旧条目。
- 文档更新必须"合并更新"：改原条目，不在底部堆"补充-日期"。
- `03-任务.md` 顶部必须维护 `> 当前焦点：T-xxx（…）`。
- 每条任务必须包含足够上下文，让新窗口的 AI 能直接执行。

## 状态枚举（只用这些词）
- 阶段：诉求对齐｜需求编写｜设计方案｜任务拆解｜执行开发
- 诉求状态：对齐中｜已锁定
- 需求状态：编写中｜已确认
- 执行：锁定｜可执行｜执行中｜已完成｜已回滚

## 输出标题块格式
```
# 🧩 <主题标题>
**【阶段：<阶段>】**
**【状态：<状态>】**
**【执行：<执行>】**
```
<!-- SPECWAVE:END core -->

## 项目自定义（留给人写）
你可以在这里补充团队约定、技术栈、发布流程等内容；升级时不会覆盖这部分。
