# SpecWave 协作说明（项目级）

<!-- SPECWAVE:START core -->
这份说明用于：让你和 AI 用自然对话推进需求，同时把关键共识落盘，方便追溯与接手。

## 协作原则
- 说不清就问：AI 没理解到位就先确认，不猜。
- 执行要授权：改业务代码/跑会产生副作用的命令前，必须等你明确说“开始/START/可以开始执行”；需求资料落盘（工作区文档）默认允许。
- 只记决定：落盘优先记录“已确认的共识/决定/边界”，不写长篇过程复盘。
- 表达要自然：先把结论与下一步说清楚，再补必要的背景与取舍；术语不可避免时顺手解释，避免只给缩写；不要在句末加“某某版/某某风格/某某口吻”之类自我标注。

## 工作区（固定）
- 需求资料统一落在 `.specwave/workspace/`。
- 项目结构真相源：`.specwave/workspace/project-map.md`（路径树 + 职责 + 边界）。

## Story（按需用，不强制）
当你需要把一次需求做成可追溯交付时，在 `.specwave/workspace/stories/` 新建 `STORY-000001(示例标题)`：
- 只维护固定文件，不另起“第二版/第三版”，避免工作区堆成文件屎山。
- 需要复用的参考资料（接口定义/测试产物/截图等）统一放在 `refs/`，避免散落在根目录或随手新建 md。

Story 目录内固定这些内容：
- `01-需求.md`：诉求一句话（开头）+ 需求（REQ）+ 验收口径（AC）（稳定编号：`REQ-001`、`AC-001`）
- `02-任务.md`：任务清单（T，必须可勾选）（稳定编号：`T-001`），每条任务回链 REQ/AC
- `03-追溯.md`：追溯表（把意图/需求/任务/实现串起来）
- `04-验收.md`：验收结论与证据（通过/不通过）
- `05-规则.md`：规则增量（只写新增/变更）
- `refs/`：参考资料专区（接口定义/测试产物/截图等）

稳定编号与维护规则（避免压缩上下文后丢进度、避免文档越写越大）：
- 编号只在 Story 内递增：`REQ-001/AC-001/T-001`；新增条目取下一个编号，不需要重排旧条目。
- 文档更新必须“合并更新”：改原条目/原任务块，不要在底部堆“补充-日期”。
- 任务状态锚点：`02-任务.md` 顶部必须维护 `> 当前焦点：T-xxx（…）`，并保证同一工作区只有 1 条任务处于“进行中”（压缩上下文后用它找回当前进度）。
- 初始化文档（`project-map.md`）只维护结论：优先改原表格行的一句话，不把过程日志硬堆进文件；长材料放到对应 Story 的 `refs/`。

## 协作阶段（不写死流程）
- 初始化：先看项目结构，再决定读哪些入口文件；项目大就先说清读取范围。
- 需求：先把效果和边界聊清，再落 REQ/AC；边聊边补骨架。
- 验收：按 AC 对照验证，给“通过/不通过”和证据；通过后才允许进入开发。
- 开发：只有“验收通过”且你说“开始”后才动手；小步推进、可回滚。

## 状态与标题（统一口径）
- 状态枚举（只用这些词，不自造）：
  - 阶段：诉求对齐｜需求编写｜需求确认｜验收｜执行｜归档
  - 诉求状态：对齐中｜已锁定
  - 需求状态：编写中｜已确认
  - 验收结论：未验收｜通过｜不通过
  - 执行：锁定｜可执行｜执行中｜已完成｜已回滚
- 输出标题块（标题 + 状态分行，便于快速扫读）：
  - 示例格式：
    - `# 🧩 <主题标题>`
    - `**【阶段：<阶段>】**`
    - `**【状态：<状态>】**`
    - `**【执行：<执行>】**`
<!-- SPECWAVE:END core -->

## 项目自定义（留给人写）
你可以在这里补充团队约定、技术栈、发布流程等内容；升级时不会覆盖这部分。
