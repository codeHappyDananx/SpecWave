# 贡献指南

感谢你为 `SpecWave` 提交改进。

## 基本原则

- 先提 issue 或讨论，再提交较大的功能改动
- 保持改动聚焦，避免把无关重构混进同一个 PR
- 新功能、行为变更、目录职责变更，需要同步更新文档
- 涉及 UI 语义的改动，先改 contracts，再改实现
- 不要移除第三方版权声明、许可证文件或 NOTICE

## 提交要求

本项目要求所有提交带有 `DCO` 签名。

请使用：

```bash
git commit -s
```

这会自动在提交信息末尾附加：

```text
Signed-off-by: 你的名字 <your@email.com>
```

提交者需要确认：

- 你有权提交该代码
- 你提交的内容可以按本项目许可证分发
- 你理解该贡献会在本仓库中按 `AGPL-3.0-or-later` 发布

## 代码与测试

- 保持变更最小且可审查
- 为新增行为补测试，避免把回归风险留给维护者
- 提交前至少运行与改动相关的检查
- 不提交构建产物、临时文件或本地环境配置

## Pull Request 检查清单

- 改动目的明确，标题和描述可读
- 已补充或更新相关文档
- 已补充或更新必要测试
- 已确认没有泄露凭证、密钥、隐私数据
- 提交已带 `Signed-off-by`

## 许可证与品牌

- 代码贡献将按 `AGPL-3.0-or-later` 分发
- `SpecWave` 名称、Logo、图标等品牌元素不随代码许可证一并授权
- 详情见 [TRADEMARKS.md](./TRADEMARKS.md)
