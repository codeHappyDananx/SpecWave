# SpecWave UI 解耦整改文档（总览）

> 目标：让“改样式/改交互”只改局部；左/中/右三区互不影响；UI 只渲染 `ViewModel`、只上报 `UIIntent`。

## 0. 现在的结论（你已经不痛苦的原因）

- 结构：Shell/Panels/Primitives 分层；三栏互不 import；跨区编排集中在 store。
- 样式：全局只保留 tokens + reset（`packages/ui-next/src/styles.css`）；组件样式全部使用 `*.module.css`。
- 欢迎页：WelcomeWindow 无边框（44px 无感拖拽区 + 自绘 `X` 退出）；最近项目持久化（默认 10 条）；背景动效每次随机。
- 稳定性：Electron 已升级到 v39；GPU/WebGL 崩溃可自救；`start.bat` 默认静默。

详细历史记录：`docs/archive/整改文档-归档-2026-01-02.md`。

## 1. 当前工作台（Main）现状：已经能做什么

- 打开项目：目录选择 → 读取项目目录 + `.specwave/workspace` → 左区渲染双树（支持懒加载）。
- 文件查看/编辑：左区点文件 → 中区显示；Markdown 支持源码/渲染切换；tasks/work 支持任务看板与“同源写回”；保存支持 sha256 if-match 冲突语义。
- 右区：终端已接入 `node-pty` + `@xterm/xterm`（基础版：多会话/输入/resize）；对话仍为示意输出。
- 布局：三栏拖拽缩放与收起；快捷键已接入（Ctrl+S 保存、Ctrl+Alt+1/2 切右区）。

## 2. 仍需关注（未关闭项/风险）

### P0：仓库公开的 license 风险（必须决策）

- `packages/ui-next/src/vendor/react-bits/*` 为 MIT + Commons Clause：公开仓库直接提交源码，存在“再分发组件本身”风险。

  - #### 1. 改造源码：将组件变为 “自有代码”（彻底规避）

    不要直接依赖 / 分发原 `react-bits` 组件，而是复制源码并定制化改造，使其成为你项目的一部分，而非 “原封不动的第三方组件”。

    **实操步骤**：

    - 复制 `packages/ui-next/src/vendor/react-bits/*` 中你用到的组件源码，粘贴到你的业务代码目录（比如 `src/components/xxx/`、`src/hooks/`），删除 `vendor/react-bits` 这个独立目录（避免被认定为 “单独存放 / 分发原组件”）；

    - 对源码做轻量化修改（非必须，但能强化 “自有代码” 属性）：

      - 重命名组件 / 钩子（比如 `useDebounce` → `useAppDebounce`）；
      - 调整内部逻辑（比如补充类型定义、修复小问题、增加业务适配逻辑）；
      - 重构代码结构（比如拆分函数、调整参数）；

    - 保留原协议的版权声明（MIT 要求），并标注修改说明（示例）：

      ```tsx
      /**
       * Derived from react-bits (https://github.com/aholachek/react-bits)
       * Original author: Alex Holachek
       * Original license: MIT + Commons Clause
       * 
       * Modified for [你的项目名称] - 仅作为应用集成使用，不单独分发该组件
       */
      export const useAppDebounce = (value, delay) => {
        // 改造后的防抖逻辑（基于原代码调整）
        const [debouncedValue, setDebouncedValue] = useState(value);
        // ... 你的定制化代码
      };
      ```

      

    - 删除项目中对 `react-bits` 的依赖（如果是 npm 安装的），确保所有引用都指向你改造后的自有代码。

    #### 2. 禁止 “单独分发组件” 的行为

    - 不要将改造前 / 后的 `react-bits` 代码单独发布为 npm 包、私有组件库；
    - 构建产物中，确保组件代码被打包进你的应用 bundle（比如 `app.js`），而非以独立文件（如 `react-bits.js`）的形式分发；
    - 公开仓库中，避免保留 `vendor/react-bits` 这类 “独立存放第三方组件” 的目录结构（直接合并到业务目录）。

    #### 3. 合规声明：明确使用边界（降低法律风险）

    如果你的项目是**开源 / 对外分发**的（比如开源应用、商业 SaaS），在项目根目录的 `LICENSE` 或 `NOTICE` 文件中补充声明：

    ```plaintext
    声明：
    本项目集成了基于 react-bits（MIT + Commons Clause）修改后的代码，未将 react-bits 组件本身作为独立产品/组件包分发，仅作为本应用的一部分集成使用。
    原 react-bits 版权归其作者所有，遵循 MIT + Commons Clause 协议；本项目自有代码遵循 [你的项目协议，如 MIT]。
    ```

- 建议：公开前移除 vendor 源码，改为依赖安装或替换为许可更宽松的动效；同时保留必要 NOTICE。

### P1：同一约束多处写死（会重新变痛苦）

面板最小宽度同时存在于 UI 和 store：

- UI：`packages/ui-next/src/shell/SpecWaveApp.tsx:34`
- store：`apps/desktop/src/renderer/src/store.ts:100`、`apps/desktop/src/renderer/src/store.ts:105`

目标口径：宽度约束只保留一处（建议由 VM/store 输出），UI 只消费。

### P1：有入口但还没实现的功能（容易被误判为 Bug）

- 主题切换：`THEME_TOGGLE` 当前 no-op。
- 搜索/查找：`GLOBAL_SEARCH_SET` 只记录 query；`SHORTCUT_FIND` 当前 no-op。
- 对话：`CHAT_MESSAGE_SUBMIT` 当前为示意输出。
- 终端：已接入 `node-pty` + `@xterm/xterm`（UIIntent：`TERMINAL_WRITE`/`TERMINAL_RESIZE`）。

## 3. 继续做（V1.x：让工作台更像工具）

- [x] 顶部项目页签：`+` 新增“空项目页签”，可在该页签内继续“打开项目”。
- [x] 关闭所有项目：MainWindow 自动回退到 WelcomeWindow（无边框）。
- [x] 顶部搜索：替换为 `SearchInput`（样式收口到 primitives，避免 focus 边框引起布局抖动）。
- [x] 左区：移除头部“文件夹”图标；字号缩小；文件夹/文件/类型更易区分（图标 + 颜色）。
- [x] 右区：页签条只占中间剩余宽度，超出横向滚动（淡滚动条，不改变高度，不撑出窗口右边界）。
- [ ] 收口布局约束来源（去掉 UI 常量，改为 VM/store 输出 minw）。
- [ ] 搜索最小闭环：顶部输入 → 左区文件名过滤/跳转（先不做全文索引）。
- [ ] Ctrl+F 查找：在中区内容区实现“当前文件查找”（终端区域不抢焦点）。
- [ ] 主题策略：要么落地 tokens 切换 + 持久化，要么隐藏入口避免误导。
- [ ] 右区真终端/真对话的接口契约先行（先补 contracts，再上实现）。

## 4. V2.0（对齐 STORY-000023 的大功能）

- 右区终端：xterm + node-pty（多会话、resize、进程退出语义、外观同步、图片粘贴落盘）。
- 对话终端：接入 Codex CLI（流式输出 + 取消 + 会话持久化）。
- 左区索引：SpecWave 工作区索引/搜索补全（增量更新、性能约束）。
- 中区编辑器：Monaco/CodeMirror（实例生命周期规范 create/update/dispose）。
- 运行时收口：ports/adapters + IPC 协议书（参数/返回/错误语义统一）。

## 5. 本轮执行计划（先落盘，细节后议）

> 目标：先把 V1.x 做成“日常可用”，再启动 V2.0 的“真终端”能力；全程保证左/中/右三区互不影响、样式不外溢。

### 5.1 待确认的口径（不影响先写计划）

- 主题：按 `SPECWAVESTYLE.MD` 是单一 Light Mode；主题按钮先隐藏还是做真正切换？
- 搜索：先做“最小闭环”，只在已加载的树节点里过滤/跳转，不额外扫盘/建索引。
- 查找：Ctrl+F 只在中区生效；终端区域不抢焦点。
- 终端：Windows 默认 shell 是否固定为 `powershell.exe`；cwd 是否固定为项目根目录。

### 5.2 V1.x（让工作台像工具）

- [x] 0.1) 顶部项目页签：`+` 新增空项目页签（未打开项目状态，可在 tab 内继续打开项目）。
- [x] 0.2) 关闭全部项目：MainWindow 自动回退到 WelcomeWindow（无边框）。
- [x] 0.3) 顶部搜索输入：接入 `SearchInput`（统一视觉 + 支持清空，样式只影响自己）。
- [x] 0.4) 左区：去掉头部“文件夹”图标；字号下调；用图标/颜色区分文件夹/文件/常见类型。
- [x] 0.5) 右区：页签溢出横向滚动（淡滚动条，不改变页签高度，不撑出窗口右边界）。
- [ ] 1) 布局约束收口：把 minWidth/70% 口径统一由 store/VM 输出，UI 不再写死常量。
- [ ] 2) 左区搜索闭环：TopBar 输入 → VM → LeftPanel 过滤/定位（不跨区、不扫盘）。
- [ ] 3) Ctrl+F 文件内查找：中区查找条 + Next/Prev + 计数；不影响右区终端输入体验。
- [ ] 4) 主题入口处理：要么落地 tokens 切换 + 持久化，要么隐藏入口避免误导。

### 5.3 V2.0 起步（右区真终端：xterm + node-pty）

- [x] 5) `packages/contracts`：补齐终端会话/事件/错误语义的契约（UIIntent + VM）。
- [x] 6) `apps/desktop` 主进程：pty manager（create/write/resize/kill）+ IPC 协议（含退出/异常）。
- [x] 7) `apps/desktop` store：接入 IPC，维护终端会话状态与输出流（不把业务逻辑写进 UI）。
- [x] 8) `packages/ui-next` 右区：xterm 渲染与生命周期（create/update/dispose），多会话切换/关闭。
- [ ] 9) 性能与资源：避免内存泄露（dispose/解绑监听/清理 pty），并补最小可观测日志（中文）。

### 5.4 回归与文档

- [x] 10) `AGENTS.md`：新增/调整文件必须登记“职责/依赖/被依赖”。
- [x] 11) `整改文档.md`：每完成一项就从“未实现清单/风险”里迁出，避免再次膨胀。
