# SpecWave UI 解耦整改文档（基线：9630226）

> 目标：让后续“改样式/改交互”变成**局部修改**，不再出现“改一个按钮影响一片”“改左区影响右区”的连锁反应。

## 1. 范围与口径

- 当前阶段：只有 UI（Electron + React + Zustand mock）。
- 解耦目标：UI 只渲染 `ViewModel`、只上报 `UIIntent`；三栏（左/中/右）逻辑与样式都要能“各改各的”。
- 单一职责口径：
  - **组件**：只负责展示与交互采集；不混入其它区域逻辑。
  - **样式**：每个组件的样式文件只服务本组件；不写“通用大类名”去污染其它组件。
  - **文件**：一文件一职责；跨区编排集中在 store（且必须写清楚原因与语义）。

## 2. 现状问题清单（按优先级）

### P0：源码目录被 `tsc` 产物污染，导致“改了不生效/误伤”

现象：源码目录内出现同名 `*.ts(x)` 与 `*.js`（以及 `*.d.ts`）并存，导入 `./App` 这类“无后缀路径”时，运行时可能优先解析到 `App.js`，造成**修改 TS 不生效**或表现随机。

- 证据：
  - 根脚本会 emit：`package.json:12`（`tsc -b`）。
  - 桌面端 tsconfig 未显式 `noEmit/outDir`：`apps/desktop/tsconfig.json:1`。
  - 源码目录已存在生成文件（示例）：
    - `apps/desktop/src/renderer/src/main.js`
    - `apps/desktop/src/renderer/src/store.js`
    - `apps/desktop/src/renderer/src/ui/App.js`
    - `apps/desktop/src/main/index.js`
    - `apps/desktop/src/preload/index.js`

整改方向：
- 通过 tsconfig 与 `.gitignore` 保证 `apps/desktop/src/**` 不再出现误生成的 `*.js/*.d.ts`（保留 `vite-env.d.ts` 这类源码声明文件）。
- 清理现存产物，避免解析混乱。

### P0：`SpecWaveApp.tsx` 单文件职责过载，拆改必然误伤

文件：`packages/ui-next/src/shell/SpecWaveApp.tsx`

问题：一个文件同时做 Shell、布局、拖拽、TopBar、三栏内容、右区终端/对话视图、以及一批“可复用组件”。

- 证据：同文件内包含多个不相干组件/职责（示例）
  - `Tab/ClosableTab/IconButton/ProjectTab`：`packages/ui-next/src/shell/SpecWaveApp.tsx:17` 起
  - `LeftPanel/CenterPanel/RightPanel`：`packages/ui-next/src/shell/SpecWaveApp.tsx:319` 起
  - `TerminalView/ChatView`：`packages/ui-next/src/shell/SpecWaveApp.tsx:545` 起

整改方向：
- Shell 与 Panels 分文件；三栏各自独立目录；可复用组件下沉到 `primitives`。

### P0：样式全局化 + 通用类名，天然“改一处崩一片”

文件：`packages/ui-next/src/styles.css`

问题：
- 同一个 CSS 同时承担 tokens、reset、基础组件样式、布局样式、三栏样式。
- 存在大量通用类名（`.btn/.tab/.panel/.input/.iconBtn`），任何调整都会波及全局。

- 证据（示例）：
  - `.btn`：`packages/ui-next/src/styles.css:54`
  - `.tab`：`packages/ui-next/src/styles.css:22`
  - `.panel`：`packages/ui-next/src/styles.css:61`
  - `.iconBtn`：`packages/ui-next/src/styles.css:44`

整改方向：
- 采用 CSS Modules（或至少 `.swRoot` 作用域 + 区域前缀），确保“组件样式只影响本组件”。

### P0：第三方动效组件 license 与“仓库公开”冲突

文件：`packages/ui-next/src/vendor/react-bits/*`

现状：当前 WelcomePage 背景动效来自 `react-bits`，其 license 为 **MIT + Commons Clause**（见 `packages/ui-next/src/vendor/react-bits/LICENSE.md`），包含“**禁止再分发组件本身**”的限制。

风险：如果本仓库未来公开（你已明确会公开），把这些组件源码直接提交到仓库，会被视为“再分发组件本身”，存在合规风险。

整改方向（按优先级给出可落地选项）：
- 方案 A（推荐，公开仓库友好）：移除 `vendor/react-bits` 源码，不再把组件源码提交到 git；改为在构建/开发时通过依赖安装（或替换为 MIT/BSD/Apache 等更宽松许可的替代动效），并保留必要的 NOTICE。
- 方案 B（继续 vendor）：仓库保持私有；或获取作者的额外授权（允许公开再分发源码）后再公开。

### P1：重复组件/交互实现未收敛到公共层

文件：`packages/ui-next/src/shell/SpecWaveApp.tsx`

问题：`ProjectTab` 与 `ClosableTab` 具有高度重复的“可聚焦 + 键盘 Enter/Space 触发 + 关闭按钮 stopPropagation”行为，但未抽公共组件。

- 证据：
  - `ClosableTab`：`packages/ui-next/src/shell/SpecWaveApp.tsx:32`
  - `ProjectTab`：`packages/ui-next/src/shell/SpecWaveApp.tsx:93`

整改方向：
- 抽统一的 `TabButton/ClosableTab` primitives，右区 tabs 与项目 tabs 复用一套行为模型。

### P1：同一约束多处写死，后续必然打架

问题：左/右最小宽度在 UI 与 store 都写了一份。

- 证据：
  - UI：`packages/ui-next/src/shell/SpecWaveApp.tsx:173`（left=240）、`:176`（right=320）
  - store：`apps/desktop/src/renderer/src/store.ts:56`（`MIN_LEFT_PX`）、`:61`（`MIN_RIGHT_PX`）

整改方向：
- 约束只保留一处（建议放 store/VM），UI 只消费 VM，不再写死常量。

### P1：AGENTS.md 尚未覆盖“每个文件职责/边界/依赖”

文件：`AGENTS.md`

现状：已列出部分关键文件，但仍缺少：
- 运行时/配置类文件的职责声明（如 `apps/desktop/electron.vite.config.ts`、`apps/desktop/src/renderer/index.html`）。
- “三栏隔离”的禁止依赖规则（例如 left/center/right 互相不得 import）。
- “生成物不维护”的明确口径（dist-electron、tsbuildinfo、src 下误生成的 js/d.ts）。

整改方向：
- 补齐每个源码文件的职责、依赖边界、修改联动点；新增文件必须同步登记。

## 3. 整改方案（落地顺序）

1) **止血：阻断 emit + 清理产物**  
   - 根 `typecheck` 仍用 `tsc -b`（project references 需要引用工程能 emit）；通过 `apps/desktop` 的 `noEmit` + packages 的 `outDir=dist`，确保不污染 `src/**`，并清理历史产物。

2) **UI 拆分：一文件一职责**  
   - Shell（布局/容器）与 Panels（左/中/右）拆开；右区终端/对话拆视图；重复控件抽 `primitives`。

3) **样式隔离：组件级样式文件**  
   - 优先 CSS Modules；全局仅保留 tokens/reset/少量基础规则；禁止通用类名外溢。

4) **边界收口：区域只拿 slice，只发本区 intents**  
   - UI 层用类型约束把 `dispatch` 限定在区域 intents；跨区行为集中在 store，并写清楚语义。

5) **补齐 AGENTS.md**  
   - 更新项目结构与“职责清单”；新增/移动文件必须同步更新。

## 4. 验收清单（做到才算“改样式不痛苦”）

- `pnpm typecheck` 后，`apps/desktop/src/**` 不再新增任何 `*.js/*.d.ts`。
- 改左区样式文件，右区/中区样式不受影响（除非改的是 tokens）。
- 改某个按钮样式，只影响该按钮（不应因为通用类名造成全局联动）。
- `packages/ui-next` 内三栏目录互相禁止依赖（只允许依赖 `contracts` 与 `primitives`）。
- `AGENTS.md` 覆盖所有源码文件的职责与边界，并跟随改动同步更新。

## 5. 执行记录（本轮改动会持续更新）

- [x] 基线提交：`chore: 基线提交（UI 初版）`（commit：9630226）
- [x] 解耦重构提交：`refactor: UI 解耦（Shell/Panels/Primitives）+ CSS Modules`（commit：c779a3f）
- [x] 止血：清理 `apps/desktop/src/**.js/.d.ts`，并通过 tsconfig 防止再生成
- [x] UI 拆分：Shell/Panels/Primitives 分离
- [x] 样式隔离：CSS Modules + 移除通用类名（全局仅保留 tokens/reset）
- [x] 更新 `AGENTS.md`：补齐文件职责与边界
- [x] 验证：`pnpm typecheck` 通过
- [x] WelcomePage：未打开项目时展示欢迎页（react-bits 背景动效），并接入“最近项目”持久化（默认 10 条；路径不存在显示警告图标，用户手动移除）
- [x] WelcomePage：背景改为“每次进入随机”（FaultyTerminal + PrismaticBurst + Hyperspeed + ColorBends + Prism），背景 DPR 按系统缩放渲染（clamp 1..2），减少不必要的过采样导致卡顿
- [x] WelcomePage：检测到软件 WebGL（SwiftShader/WARP）或发生 `CONTEXT_LOST_WEBGL` 时，自动切到 CSS 背景动效，避免黑屏/低帧率拖垮体验
- [x] GPU：主进程调用 `app.disableDomainBlockingFor3DAPIs()`，避免 GPU 崩溃后 Chromium 按域封禁 WebGL（现象常表现为“动画像没生效/只剩黑底”）
- [x] WelcomePage：移除最近项目卡片的 `backdrop-filter`（动效 + blur 组合会显著拖慢帧率，并放大显卡不稳定概率）
- [x] WelcomePage：进一步减少 blur 成本（移除背景层的 `filter: blur(...)`），降低“首页很卡”的概率
- [x] react-bits：统一补齐 WebGL 初始化失败/丢上下文日志，并在 `CONTEXT_LOST_WEBGL` 时停帧；同时输出 WebGL 渲染器信息/首帧耗时/FPS 采样，方便定位“到底是不是 GPU/SwiftShader 问题”
- [x] WelcomePage：WebGL 不可用时在页面内给出可关闭提示，避免出现“空白但无报错”的错觉
- [x] GPU：补充启动开关 `SPECWAVE_USE_GL`/`SPECWAVE_DISABLE_GPU_SANDBOX`/`SPECWAVE_RESET_GPU_PREFS`，并把生产模式 GPU 自救扩展为 d3d11 → d3d11+disable-gpu-sandbox → d3d9 → warp → swiftshader-webgl → disable-gpu
- [x] GPU：生产模式自救结果持久化到 `userData/gpu-preferences.json`（避免用户每次启动都要手动切 ANGLE；需要重置可用 `SPECWAVE_RESET_GPU_PREFS=1`）
- [x] GPU：开发模式（Windows）默认 `ANGLE=d3d11`（优先硬件加速；若显卡崩溃可用 `start.bat d3d9|warp` 排查）
- [x] start.bat：一键启动支持模式参数（d3d11/d3d9/warp/swiftshader/nogpu），默认清理残留进程，并使用 `.tmp-specwave-userdata/` 隔离开发 profile（默认 `ANGLE=d3d11`）
