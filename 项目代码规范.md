# 项目代码规范

> 只在"改代码/写文件/跑有副作用命令"时生效

---

## 1. 排障策略（Probe First）

当复现路径不清晰或置信度不高时：

1. 先在关键入口/出口/分支点加带上下文的日志（如 `console.log('Step', x, data)`）
2. 流程：加日志 → 运行复现 → 读日志 → 定位根因 → 移除日志 → 正式修复

**禁止"盲改业务代码赌运气"**

---

## 2. 样式与字体规范（硬约束）

### 2.1 字体变量（必须）

所有新增的字体样式必须使用项目定义的字体变量，禁止直接写字体名称或使用其他变量：

- **正文字体**：`font-family: var(--sw-font-sans);`
- **等宽字体**：`font-family: var(--sw-font-mono);`

字体变量定义在 `packages/ui-next/src/styles.css`：
```css
--sw-font-sans: "Outfit", system-ui, -apple-system, "Segoe UI", "Helvetica Neue", Arial, "Noto Sans",
  "Microsoft YaHei", "PingFang SC", sans-serif;
--sw-font-mono: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono",
  "Courier New", monospace;
```

**禁止行为**：
- ❌ `font-family: monospace;`
- ❌ `font-family: var(--font-mono, monospace);`
- ❌ `font-family: "Arial", sans-serif;`

**正确写法**：
- ✅ `font-family: var(--sw-font-sans);`
- ✅ `font-family: var(--sw-font-mono);`

---

## 3. 代码注释与日志（硬约束）

**目标**：代码交付后，任何接手的人都能"只看中文注释和中文日志"把关键口径复盘清楚。

### 3.1 什么算"关键处理节点"

- 权限/风控/资金/账务等口径落地处（尤其是"返回空 vs 抛异常 vs 放开"的分界点）
- 跨服务调用（RPC/MQ/HTTP）与其降级/兜底策略
- 动态 SQL 拼接/注入点、拦截器、过滤器、AOP 等"全局生效"的入口
- 缓存命中/失效策略（包含"不使用缓存"的明确选择与原因）

### 3.2 注释要求（必须）

- 关键处理节点必须写中文注释，至少覆盖：口径、输入来源、输出语义、异常/降级策略、为什么这么做
- 注释优先讲"业务含义"，少讲实现细节；必要时把"坑点/边界条件"写出来
- 不写"空话注释"（如"获取数据""处理逻辑"）；要写能指导排查的内容

### 3.3 日志要求（必须）

- 关键日志必须中文，且带上下文：关键入参、模式/开关、降级原因、返回语义
- 禁止用英文缩写把口径藏起来（如只打印 `mode=3` 不说明含义）

### 3.4 复杂度控制（必须）

- 公共方法优先"直给写法"，能看懂为第一目标
- 确需抽方法时只抽 1–2 个真正复用点
- 避免为"优雅"拆出一堆辅助方法导致读代码跳来跳去

---

## 4. 代码性能可用要求（必须）

站在"这个接口是对外暴露的交易接口"的想法：

- **禁止循环内查库**：非特别允许，不得在循环内调用数据库查询
- **线程安全**：用到线程需要考虑线程安全问题
- **不造轮子**：项目中已有类似用法，不允许自己写工具；确实不满足时，在相同类型的文件/类中加可用方法
