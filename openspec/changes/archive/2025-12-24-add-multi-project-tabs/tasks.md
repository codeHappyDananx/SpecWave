## 1. 实施
- [x] 1.1 新增页签状态管理（最多 5 个，支持新建/切换/关闭）
- [x] 1.2 项目状态按页签隔离（每个页签独立 project store）
- [x] 1.3 终端状态按页签隔离（每个页签独立 TerminalPanel，会话互不影响）
- [x] 1.4 主进程目录监听按页签隔离（watch-directory 支持 key，多 watcher，并回传 key）
- [x] 1.5 自定义菜单（项目 -> 新建页签 / 打开最近 / 退出；菜单事件透传到渲染进程）
- [x] 1.6 冒烟：打开最近 -> 自动新建页签；切换页签不串状态；关闭页签释放资源

## 2. 验证
- [x] 2.1 运行构建与类型检查
- [x] 2.2 手动冒烟（最多 5 个页签；终端会话隔离；最近项目菜单动态更新）
